'For easy modification of application name and version
'These are in separate file, so that they can be included in CLI tools
'which don't use the main library

    CONST appName = "The Costa Graphical Shell"
    CONST appVersion = "1.6.0"
    CONST appYear = "2022"
    CONST appEnviron = "COSTA160"
    CONST appExeName = "DESKTOP" 'Without extension

